import React from 'react';
import { ToastContainer } from 'react-toastify';

const ToastSuccess = () => {
    return (
        <ToastContainer>
            to
        </ToastContainer>
    );
};

export default ToastSuccess;